[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oste-moe"
version = "1.0.0"
description = "Hierarchical Multi-Modal Mixture-of-Experts Exoplanet Discovery Engine"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
